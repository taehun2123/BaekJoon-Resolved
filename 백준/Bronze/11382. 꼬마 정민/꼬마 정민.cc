#include <iostream>
using namespace std;
long long a;
long long b;
long long c;

int main() {
    cin >> a >> b >> c;
    long long sum = 0;
    sum = a + b + c;
    cout << sum;
    return 0;
}
